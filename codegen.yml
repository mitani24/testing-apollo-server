overwrite: true
schema: ./schema.graphql
generates:
  ./src/lib/generated/graphql-resolver-types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      avoidOptionals: true
